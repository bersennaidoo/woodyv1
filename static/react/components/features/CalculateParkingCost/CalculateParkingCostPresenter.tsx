import React, { FC } from "react"

interface ICalculateParkingCostProps {}

const CalculateParkingCostPresenter: FC<ICalculateParkingCostProps> = (props: ICalculateParkingCostProps) => {

    const {} = props

    return (
        <form>
            formcontrol
        </form>
    )
}

export default CalculateParkingCostPresenter